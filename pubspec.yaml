name: visor
description: Vision training app — Gabor patch discrimination games and eye exercises.
publish_to: none
version: 0.1.1+2
environment:
  sdk: ^3.13.0
dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.3+1
  path: ^1.9.0
  cupertino_icons: ^1.0.8
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true