# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
build/
*.iml
.idea/
.metadata

# Signing secrets — never commit
keystore/
*.keystore
*.jks
android/key.properties

# Environment / credentials — never commit
.env

# Stale package-path stub (active package is com.visor.app)
android/app/src/main/kotlin/com/visor/visor/

# OS
.DS_Store
# Private local notes — not published
TODO.md

# Draft screenshots — not published
assets/previews/

# Build / deploy staging & scratch — not published
.cf-site/
.cf_deploy.py
visor-landing/
visor_patch_setup.py
build_release.log
local_build_test.log
rebuild2.log
# Generated banner experiments (the published banners live in visor-assets/banner_*, graphic_*)
visor-assets/banner_A*.png
visor-assets/banner_B*.png


# Unused CI draft (native CF GitHub App, not Actions)
.github/
